<div class="question_content top_content">
  <div class="question_body">
    <div class="question_box">
      <span class="q">Q</span>
      <apan class="question_text"><%= question.text %></apan>
    </div>
    <div class="answer_box" style="margin-bottom: 10px;">
      <span class="a">A</span>
      <span class="answer_text"><%= question.user_answer(@user.id).text %></span>
      <%#ここで呼び出しているuser_answerメソッドは、ある特定のquestionのidとuserのidを用いて、指定の質問に対するユーザーの回答をanswersテーブルから取得するメソッド。一つの質問、つまりQuestionクラスのインスタンスに対して使用するため、Questionモデルにこのメソッドは定義する。%>
      <% if @user.id == current_user.id %>
      <small class="edit">
        <%= link_to "編集", edit_answer_path(question.user_answer(@user.id)) %>
      </small>
      <% end %>
    </div>
  </div>
</div>
